# SciELO Log Validator

```bash
usage: validator.py [-h] -p PATH [-s SAMPLE_SIZE] [--check_file_name_only]

optional arguments:
  -h, --help            show this help message and exit
  -p PATH, --path PATH  arquivo ou diretório a ser verificado
  -s SAMPLE_SIZE, --sample_size SAMPLE_SIZE
                        tamanho da amostra a ser verificada
  --check_file_name_only
                        indica para validar apenas o nome e o caminho do(s) arquivo(s)
```